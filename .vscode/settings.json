{
    "typescript.tsdk": "node_modules/typescript/lib",
    "jest.runMode": {
        "type": "on-save",
        "runAllTestsOnStartup": false
    },
    "jest.debugMode": true,
    // workaround for weird jest bug
    "jest.jestCommandLine": "source /usr/share/nvm/init-nvm.sh && nvm use --lts && npm run test --",
    "jest.useDashedArgs": true,
    "jest.shell": {
        "path": "/bin/bash",
        "args": [
            "--login"
        ]
    }
}
